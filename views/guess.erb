<p>Guess the <%= solution.length %>-letter word!</p>
<div>

</div>

<p><%= revealed_letters %></p>

<p>Previous guesses: <%= all_guesses %></p>

<% if guesses_remaining > 1 %>
  <p>You have <%= guesses_remaining.to_s %> guesses remaining.</p>
<% else %>
  <p>You have <%= guesses_remaining.to_s %> guess remaining.</p>
<% end %>

<form action="/guess", method="POST">
  Guess a letter (a-z):
  <input type="text" name="guess" maxlength="1" pattern="[A-Za-z]" required/><br>
  <input type="submit" name="submit">
</form>

<a href="/">Main Menu</a>
